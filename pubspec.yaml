name: proctorlyflutter
description: "flutter version of the Proctorly App"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.7+4
  device_preview: ^1.1.0
  cupertino_icons: ^1.0.8
  supabase_flutter: ^1.0.0 # Add Supabase dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png  # Add your image path here
    - assets/profile_placeholder.png # Placeholder for profile picture
